# ═══════════════════════════════════════════════════════════════
#  AfDB Platform — GitHub Actions CI/CD
# ═══════════════════════════════════════════════════════════════
#  Automatically deploys to AWS EC2 when code is pushed to main.
#
#  Runner: self-hosted "aws-ec2-afdb" on EC2
#  Docker Hub: deputee/afdb (single repo, tags per service)
#
#  Required GitHub Secrets:
#    DOCKERHUB_USERNAME  — Docker Hub username (deputee)
#    DOCKERHUB_TOKEN     — Docker Hub personal access token
#    MONGODB_URI         — MongoDB Atlas connection string
#    REDIS_PASSWORD      — Redis password
#    JWT_ACCESS_SECRET   — JWT access token secret
#    JWT_REFRESH_SECRET  — JWT refresh token secret
#    INTERNAL_API_KEY    — Internal API key (beta ↔ core)
#    SSO_SHARED_SECRET   — SSO federation shared secret
#    SMTP_PASSWORD       — SMTP email password
#    DOMAIN              — Production domain
#    CORS_ORIGINS        — Allowed CORS origins
# ═══════════════════════════════════════════════════════════════

name: Deploy to AWS

on:
  push:
    branches: [main]
  workflow_dispatch: # Allow manual trigger

jobs:
  # ── Job 1: Build & Push to Docker Hub ───────────────────────
  build:
    name: Build & Push Images
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Validate Docker Hub credentials
        run: |
          # Trim whitespace from token
          TOKEN=$(echo -n "${{ secrets.DOCKERHUB_TOKEN }}" | tr -d '[:space:]')
          USERNAME=$(echo -n "${{ secrets.DOCKERHUB_USERNAME }}" | tr -d '[:space:]')

          if [ -z "$TOKEN" ] || [ -z "$USERNAME" ]; then
            echo "✗ ERROR: DOCKERHUB_TOKEN or DOCKERHUB_USERNAME secret is empty"
            exit 1
          fi

          # Test login via Docker Hub API
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
            -X POST \
            -H "Content-Type: application/json" \
            -d "{\"username\": \"${USERNAME}\", \"password\": \"${TOKEN}\"}" \
            https://hub.docker.com/v2/users/login/)

          if [ "$HTTP_CODE" = "200" ]; then
            echo "✓ Docker Hub credentials are valid"
          else
            echo "✗ ERROR: Docker Hub login failed (HTTP ${HTTP_CODE})"
            echo "  → Go to https://hub.docker.com/settings/security"
            echo "  → Generate a new Personal Access Token"
            echo "  → Update DOCKERHUB_TOKEN in GitHub Secrets"
            exit 1
          fi

      - name: Login to Docker Hub
        run: |
          TOKEN=$(echo -n "${{ secrets.DOCKERHUB_TOKEN }}" | tr -d '[:space:]')
          USERNAME=$(echo -n "${{ secrets.DOCKERHUB_USERNAME }}" | tr -d '[:space:]')
          printf "%s" "$TOKEN" | docker login -u "$USERNAME" --password-stdin

      - name: Build and push beta-backend
        run: |
          USERNAME=$(echo -n "${{ secrets.DOCKERHUB_USERNAME }}" | tr -d '[:space:]')
          docker build -t ${USERNAME}/afdb:beta-backend-latest -t ${USERNAME}/afdb:beta-backend-${{ github.sha }} .
          docker push ${USERNAME}/afdb:beta-backend-latest
          docker push ${USERNAME}/afdb:beta-backend-${{ github.sha }}
          echo "✓ Pushed ${USERNAME}/afdb:beta-backend-latest"
          echo "✓ Pushed ${USERNAME}/afdb:beta-backend-${{ github.sha }}"

  # ── Job 2: Deploy on EC2 via Self-Hosted Runner ────────────
  deploy:
    name: Deploy on EC2
    runs-on: aws-ec2-afdb  # Self-hosted runner on EC2
    needs: build
    environment: production

    steps:
      - name: Login to Docker Hub
        run: |
          TOKEN=$(echo -n "${{ secrets.DOCKERHUB_TOKEN }}" | tr -d '[:space:]')
          USERNAME=$(echo -n "${{ secrets.DOCKERHUB_USERNAME }}" | tr -d '[:space:]')
          printf "%s" "$TOKEN" | docker login -u "$USERNAME" --password-stdin

      - name: Pull latest image from Docker Hub
        run: |
          USERNAME=$(echo -n "${{ secrets.DOCKERHUB_USERNAME }}" | tr -d '[:space:]')
          docker pull ${USERNAME}/afdb:beta-backend-latest

      - name: Stop existing container
        run: |
          docker stop afdb-beta-backend 2>/dev/null || true
          docker rm afdb-beta-backend 2>/dev/null || true

      - name: Run beta-backend container
        run: |
          USERNAME=$(echo -n "${{ secrets.DOCKERHUB_USERNAME }}" | tr -d '[:space:]')
          docker run -d \
            --name afdb-beta-backend \
            --restart always \
            -p 4000:4000 \
            -e PORT=4000 \
            -e NODE_ENV=production \
            -e MONGODB_URI="${{ secrets.MONGODB_URI }}" \
            -e REDIS_HOST="${{ secrets.REDIS_HOST }}" \
            -e REDIS_PORT=6379 \
            -e REDIS_PASSWORD="${{ secrets.REDIS_PASSWORD }}" \
            -e JWT_ACCESS_SECRET="${{ secrets.JWT_ACCESS_SECRET }}" \
            -e JWT_REFRESH_SECRET="${{ secrets.JWT_REFRESH_SECRET }}" \
            -e JWT_ACCESS_EXPIRY=30m \
            -e JWT_REFRESH_EXPIRY=30d \
            -e SSO_SHARED_SECRET="${{ secrets.SSO_SHARED_SECRET }}" \
            -e SSO_TOKEN_EXPIRY_MINUTES=5 \
            -e CORE_API_URL="${{ secrets.CORE_API_URL }}" \
            -e CORE_API_KEY="${{ secrets.INTERNAL_API_KEY }}" \
            -e CORS_ORIGINS="${{ secrets.CORS_ORIGINS }}" \
            -e SMTP_HOST="${{ secrets.SMTP_HOST }}" \
            -e SMTP_PORT="${{ secrets.SMTP_PORT }}" \
            -e SMTP_USER="${{ secrets.SMTP_USER }}" \
            -e SMTP_PASSWORD="${{ secrets.SMTP_PASSWORD }}" \
            -e SMTP_FROM="${{ secrets.SMTP_FROM }}" \
            ${USERNAME}/afdb:beta-backend-latest

      - name: Verify container is running
        run: |
          echo "→ Container status:"
          docker ps --filter name=afdb-beta-backend --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
          echo ""
          echo "→ Container logs (last 10 lines):"
          docker logs --tail 10 afdb-beta-backend
          echo ""
          echo "✓ Deployment complete!"

      - name: Cleanup old images
        run: docker image prune -f
